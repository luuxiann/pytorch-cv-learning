# Day 2
教程：
https://www.runoob.com/python/python-tutorial.html
https://pytorch.org/docs/stable/index.html
https://pytorch.org/vision/stable/index.html
https://www.bilibili.com/video/BV1rVpWz3EDW?spm_id_from=333.788.videopod.episodes&vd_source=301feeee8ee482b3882c9f098e564491&p=2
## 完成任务
1. python控制流、函数等学习(具体学习笔记见python_learning.md)
2. pytorch相关的环境配置

## 遇到的问题
由于学习的教程用的是Python 2.0+，而我用的是Python 3.12，所以在运行教程里的代码时要修改其print部分的代码，主要是添加括号。当然还有比如在Python 3+中，没有softspace属性的情况了，这时运行就会报错。

### 配置过程
配置ubuntu

```
sudo apt update
sudo apt install build-essential
sudo apt install python3.10         # 因为用的是Ubuntu 22.04.4，所以对应安装的是3.10，教程里的是3.8
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh
bash                                         # 进入空环境
pip install jupyter d2l torch torchvision    # 记事本，library
# 由于Python 3.13与numpy 1.23.5不兼容导致安装错误
# 解决
conda deactivate                            # 退出当前环境（如果是base环境）
conda create -n pytorch-env python=3.9      # 创建一个新的conda环境，指定Python 3.9或3.10（这些版本与PyTorch兼容性好）
conda activate pytorch-env                  # 激活新环境
pip install jupyter d2l torch torchvision   # 重新安装包 ps这个方法装torch真的好慢
```
